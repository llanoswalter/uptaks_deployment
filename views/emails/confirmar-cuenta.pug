doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
            
    body
        h2 Confirma tu cuenta UpTaks
        p Ya estas casi inscripto, solo presiona el siguiente boton
        a(href=`${confirmarUrl}`) Confirmar cuenta
        p Si no puede acceder a este enlace, vicita #{confirmarUrl}
